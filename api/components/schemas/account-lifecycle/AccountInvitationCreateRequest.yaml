type: object
additionalProperties: false
required:
  - scope
  - full_name
  - email
  - role_ids
properties:
  scope:
    type: string
    enum:
      - platform
      - school
  school_id:
    type:
      - string
      - 'null'
    format: uuid
  full_name:
    type: string
    minLength: 1
    maxLength: 255
  email:
    type: string
    format: email
    maxLength: 255
    description: >-
      Trimmed and lowercased by the server before validation and identity
      lookup, and before storage when platform invitation creation provisions a
      missing platform-owned user. Global ownership includes soft-deleted users.
  role_ids:
    type: array
    minItems: 1
    items:
      type: string
      format: uuid
  delivery_metadata:
    type: object
    additionalProperties: true
