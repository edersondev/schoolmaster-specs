name: document
in: query
description: Filter schools by exact canonical CNPJ/document digits after trimming and removing punctuation. No `cnpj` query alias is supported.
schema:
  type: string
  minLength: 1
  maxLength: 32
  example: '12.345.678/0001-95'
