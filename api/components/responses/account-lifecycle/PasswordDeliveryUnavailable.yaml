description: Password email submission is temporarily unavailable and may be retried
content:
  application/json:
    schema:
      $ref: ../../schemas/common/ErrorEnvelope.yaml
    example:
      error:
        code: temporary_unavailable
        message: Password email could not be submitted. Try again.
        details: {}
