description: Password delivery limit or reset-token suppression is active
content:
  application/json:
    schema:
      $ref: ../../schemas/common/ErrorEnvelope.yaml
    example:
      error:
        code: password_delivery_rate_limited
        message: Password delivery is temporarily limited. Try again later.
        details: {}
