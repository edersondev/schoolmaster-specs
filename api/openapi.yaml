openapi: 3.1.0
info:
  title: SchoolMaster API
  version: 0.1.0
  summary: Contract placeholder for the SchoolMaster platform API.
  description: >
    This OpenAPI document is the contract between the SchoolMaster frontend and
    backend repositories. The target implementations are a Laravel API and a
    Vue 3 SPA. Detailed endpoint definitions remain pending approval from the
    specification artifacts under specs/.
servers:
  - url: /api/v1
    description: Versioned API base path
tags:
  - name: Pending
    description: Placeholder tag until module contracts are promoted here
paths: {}
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
x-notes:
  source_of_truth: schoolmaster-specs
  backend_repository: schoolmaster-backend
  frontend_repository: schoolmaster-frontend
  status: Placeholder contract. Promote approved operations from specs/*/contracts/openapi.yaml.
